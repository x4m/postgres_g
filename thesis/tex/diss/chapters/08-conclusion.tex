\chapter*{Заключение}
\addcontentsline{toc}{chapter}{Заключение}

В работе решена задача снижения вычислительной и вводо-выводной стоимости
построения, поиска и сопровождения индексов многомерных данных за счёт развития
моделей и алгоритмов обобщённых деревьев поиска. Основные результаты:

\begin{enumerate}

\item Построены аналитические модели стоимости операций над обобщённым деревом
поиска (глава~\ref{ch:models}), связывающие ветвистость узла, перекрытие ключей
и размерность данных с числом обращений к страницам. Из моделей следует, что
перекрытие ключей не накапливается с глубиной дерева, а выходит на характерное
для стратегии разбиения установившееся значение, и потому входит в стоимость
поиска постоянным множителем на каждом шаге спуска; ветвистость же влияет на
стоимость поиска логарифмически, а на объём индекса и стоимость массовых
операций~--- обратно пропорционально. Отсюда определены три
параметра, поддающиеся воздействию без изменения класса операторов: ветвистость,
перекрытие и порядок обращения к страницам.

\item Разработаны алгоритмы повышения ветвистости узла
(глава~\ref{ch:fanout}): отказ от обязательных функций преобразования ключа с
приданием их отсутствию семантики тождественного представления; разделение
атрибутов индекса на ключевые и покрывающие, позволяющее включить в индекс
столбцы, тип которых не имеет класса операторов, не увеличивая высоту дерева;
перезапись элемента на странице без перемещения данных. Показано, что стоимость
просмотра узла растёт линейно по ветвистости, что ограничивает выигрыш от её
роста и требует внутристраничного индексирования.

\item Разработан алгоритм построения обобщённого дерева поиска сортировкой
(глава~\ref{ch:sorted-build}). Порядок, сохраняющий локальность, объявляется
классом операторов через интерфейс поддержки сортировки, что делает приём
применимым к произвольному классу операторов, а не только к R-дереву. Выявлен и
устранён основной побочный эффект метода~--- рост перекрытия нелистовых ключей:
точки разбиения выбираются функцией разбиения узла над буфером в четыре
страницы, а не по границе заполнения страницы. Определены границы применимости
метода.

\item Разработаны алгоритмы конкурентного сопровождения индекса
(глава~\ref{ch:maintenance}): обход в физическом порядке страниц вместо
логического; двухпроходное удаление опустевших страниц с возвратом их в оборот;
формулировка условия безопасности переиспользования через полный
идентификатор транзакции, не подверженный зацикливанию; сужение области и
отложенный захват блокировок при сборке мусора в обратном индексе; страничная
гранулярность предикатных блокировок для сериализуемой изоляции.

\item Разработан метод верификации структурных инвариантов индексных структур
(глава~\ref{ch:verification}), пригодный для промышленной эксплуатации:
инварианты формулируются в терминах интерфейса класса операторов; проверка
согласованности ключей родителя выполняется с расходом памяти порядка ширины
уровня; проверка соседних ссылок выполняется со сцеплением блокировок в
однонаправленном порядке; сравнение кортежей выполняется после нормализации
представления; проверка допускает выполнение на реплике.

\item Все перечисленные алгоритмы реализованы в СУБД \pg{}, приняты в её
основную ветку и поставляются в составе выпусков 10--19
(приложение~\ref{app:commits}). Проведена экспериментальная оценка
эффективности и установлены границы применимости
(глава~\ref{ch:evaluation}).

\end{enumerate}

\section*{Рекомендации и перспективы дальнейшей разработки темы}

\begin{itemize}

\item \emph{Внутристраничное индексирование} (§\ref{sec:intrapage}). Снятие
линейной по ветвистости составляющей стоимости просмотра узла сделало бы
увеличение размера страницы безусловно выгодным и изменило бы баланс
параметров, установленный главой~\ref{ch:models}. Основное затруднение~---
стоимость поддержания внутристраничной структуры при вставках.

\item \emph{Верификация обобщённого дерева поиска} в полном объёме
(глава~\ref{ch:verification}): для обратного индекса проверка реализована,
для обобщённого дерева поиска работа не завершена.

\item \emph{Слияние страниц при сборке мусора} для B-дерева: возврат страниц в
оборот, реализованный в главе~\ref{ch:maintenance} для обобщённого дерева
поиска, не решает задачу частично заполненных страниц.

\item \emph{Удаление страниц дерева вхождений обратного индекса без избыточных
блокировок}~--- продолжение §\ref{sec:gin-locks}.

\item \emph{Параллельное построение индекса}: сортировка, к которой сведено
построение (глава~\ref{ch:sorted-build}), распараллеливается естественным
образом, и это направление остаётся неисследованным для обобщённого дерева
поиска.

\end{itemize}

\section*{Благодарности}
\addcontentsline{toc}{section}{Благодарности}

% Составлено по трейлерам коммитов (Author / Reviewed-by / Reported-by) и по
% составу тредов, а не по памяти. Проверить написание имён, стоящих в архиве
% кириллицей.

Все вошедшие в работу результаты проходили через открытое рецензирование в
сообществе PostgreSQL, и почти всякий из них после рецензирования выглядел
иначе, чем до него. Ниже названы те, чья работа это была; принадлежность к
организациям опущена, поскольку она за годы работы менялась.

За рецензирование и принятие результатов глав~\ref{ch:sorted-build}
и~\ref{ch:maintenance} автор благодарит Хейкки Линнакангаса, доводившего
сопровождение обобщённого дерева поиска с~2017 по~2019 год и переписавшего
часть кода к лучшему; Мелани Плейджмен~--- за упреждающее чтение в трёх методах
доступа; Теодора Сигаева и Джеффа Дэвиса~--- за работу с блокировками обратного
индекса; Александра Короткова~--- за отмену неверного изменения и за два
последовавших исправления взаимных блокировок.

Отдельная благодарность Джеффу Джейнсу: проверка сборки мусора на устойчивость
к разрыву страниц при высокой конкурентности и измерения на оборудовании,
которого у автора не было, выполнены им по собственному почину. Приведённые в
§\ref{sec:physical-order} значения для жёсткого диска~--- его.

Задача главы~\ref{ch:sorted-build} поставлена Дарафеем Пралясковским, собравшим
для неё распределённый хакатон; в нём участвовали Александр Каленик и Сергей
Шулбаков. Работа над сопровождением продолжает предложение Константина
Кузнецова 2015~года.

За рецензирование, ведение патчей через комиссии и участие в обсуждениях автор
благодарит Александра Короткова, Томаса Манро, Михаэля Пакье, Дмитрия Долгова,
Дэвида Стила, Михаила Николаева, Роберта Хааса, Питера Гейгана, Амита Капилу,
Дилипа Кумара, Павла Борисова, Андреса Фройнда, Бернда Хельмле, Джунванга Чжао
и Кирилла Решке.

Дефект, отменивший результат §\ref{sec:gin-revert}, обнаружен в эксплуатации и
сообщён Чэнем Хуацзюнем. Без этого сообщения ошибка оставалась бы незамеченной
дольше, чем осталась,~--- и глава~\ref{ch:verification}, выросшая из этого
эпизода, вероятно не была бы написана.

Ответственность за всё ошибочное в работе, включая отменённый алгоритм, лежит
на авторе.
