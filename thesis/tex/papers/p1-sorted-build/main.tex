% P1 — целевая площадка PVLDB. План доработки: ../../plan/60-pvldb.md
% Постановка задачи поднята на уровень фреймворка: не «быстрее строить
% R-дерево», а «что фреймворк обязан затребовать от класса операторов, чтобы
% массовая загрузка стала возможной и не ухудшала качество дерева».
%
% Перед подачей: перевести на vldb.cls, ~12 страниц, сверить актуальные
% правила тома (объём, анонимность, артефакты).

\documentclass[11pt, a4paper]{article}

\usepackage{fontspec}
\usepackage[english, russian]{babel}
\selectlanguage{english}
\setmainfont{Times New Roman}[Ligatures=TeX]
\setmonofont{Menlo}[Scale=0.85]

\usepackage{geometry}
\geometry{a4paper, margin=25mm}
\usepackage{amsmath, amssymb, graphicx, booktabs, listings}
\usepackage[hidelinks]{hyperref}

\lstset{basicstyle=\ttfamily\small, breaklines=true, frame=single}
\newcommand{\code}[1]{\texttt{#1}}
\emergencystretch=1em

\newtheorem{claim}{Claim}
\newtheorem{definition}{Definition}

\title{Bulk Loading in Extensible Index Frameworks}
\author{Andrey Borodin \and Sergey Porshnev}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Bulk loading a tree index by sorting the input and packing pages is a technique
known since the 1980s, and packing algorithms for R-trees are textbook
material. All of them, however, are formulated for a particular key type and
rely on its geometric properties. Extensible index frameworks~--- of which the
generalized search tree (GiST) is the most widely deployed instance~--- expose
no such properties: an operator class defines containment, union, penalty and
split, and nothing else. Consequently, indexes built on these frameworks have
been constructed by repeated insertion, at $O(N \log_f N)$ random page accesses
and with pages filled to roughly 70\,\%.

We study bulk loading at the level of the framework: what an extensible index
framework must ask of an operator class for sorting-based construction to be
possible, and what it must do with the answer so that the resulting tree is no
worse than one built by insertion. The first part is a single additional
support function returning a comparator. The second is the substantive one: we
show that packing pages by occupancy systematically inflates the overlap of
non-leaf keys, and we characterise the inflation. Each partitioning strategy
converges to a \emph{steady-state} overlap $\omega_\infty$~--- the expected
number of nodes per level whose key matches a point query~--- reached one or
two levels below the root and stable thereafter. Occupancy-based packing yields
$\omega_\infty \approx 4$, so a query descends into four subtrees at each level
instead of one; insertion-based construction, the baseline it replaces, yields
$\omega_\infty \approx 1.05$~--- essentially a single path. The penalty is
therefore a constant factor at every step of the descent: the index is built
6.8--8.0$\times$ faster and answers point queries 2.4--2.5$\times$ more
expensively. We give an algorithm that delegates the choice of split points
back to the operator class over a multi-page buffer; it removes 87--89\,\% of
the penalty, bringing query cost within 18\,\% of insertion-based construction
while retaining a 3.0--3.5$\times$ speedup of the build.

The design is implemented in PostgreSQL, has shipped since release~14, and has
been the default strategy for a family of operator classes since release~18. We
evaluate it on \_\_ datasets and \_\_ operator classes, against insertion-based
construction, naive sorted construction and Hilbert-order packing, and we
report a five-year deployment history including the one case in which the
correctness condition of the method was violated in production.
\end{abstract}

\textbf{PVLDB Reference Format:} % TODO по шаблону тома
\textbf{PVLDB Artifact Availability:} % TODO — код в выпусках PostgreSQL 14/15/18

\section{Introduction}

Database systems have long stopped treating multidimensional data as a
specialty. Geometric and geographic objects, ranges, full-text signatures and
vector representations are handled by general-purpose systems alongside scalar
types. No single tree structure serves all of them, and systems have responded
with frameworks rather than structures: the generalized search tree
\cite{hellerstein1995generalized} implements page layout, descent, split, the
concurrency protocol \cite{kornacker1997concurrency} and write-ahead logging,
and delegates the data-specific decisions to an \emph{operator class}.
Substituting bounding rectangles yields an R-tree \cite{guttman1984r};
substituting signatures yields a full-text index; substituting ranges yields a
range index. This is what makes an index framework extensible by users rather
than only by the authors of the system
\cite{chilingarian2004postgresql,rogov2026}.

Extensibility is bought with ignorance. The framework knows containment
(\code{consistent}), key combination (\code{union}), the cost of extending a
key (\code{penalty}) and node partitioning (\code{picksplit}), and nothing
else~--- in particular, no order on keys, and no way to derive one. Bulk
loading, which for a B-tree means sorting the input and packing pages, is
therefore unavailable, and indexes on such frameworks are built the only way
that requires no order: by inserting entries one at a time. Every insertion
descends to a leaf, so construction costs $O(N \log_f N)$ page accesses; the
accesses are random, because insertion order is unrelated to physical
placement; and pages end up filled to about 70\,\%, because they are produced
by splits rather than by packing.

The gap is conspicuous because bulk loading of R-trees is well understood.
Packed R-trees \cite{roussopoulos1985direct}, Hilbert packing
\cite{kamel1993hilbert} and sort-tile-recursive packing
\cite{leutenegger1997str} all sort by some function of the key and fill nodes
consecutively. None of them applies here: each is formulated in terms of
coordinates or of the area of a bounding rectangle, and an operator class over
signatures or over ranges offers neither.

\paragraph{Problem.} We therefore ask the question one level up. Given an
extensible index framework, (i)~what is the minimal additional information the
framework must obtain from an operator class for sorting-based construction to
be possible, and (ii)~what must the framework do with that information so that
the resulting tree is no worse, for queries, than one built by insertion?

The answer to (i) turns out to be small: one optional support function
returning a comparator over keys. The order need not be total in any meaningful
sense, and it need not be derivable from the query semantics; it must merely
preserve locality, and only the operator class can know which order does.

The answer to (ii) is the substance of this paper, because the obvious
procedure is wrong in a way that measurement of construction time cannot
reveal. Filling pages to capacity and starting a new page chooses split points
by a single criterion~--- occupancy. In one dimension that criterion is
optimal. In more than one it is not: an order that preserves locality does so
only approximately, and a page boundary that falls inside a dense region
produces two neighbouring pages whose keys overlap substantially. Overlap
determines the cost of search in a generalized tree: a query descends into
every subtree whose key matches it. Measured per level, the overlap of an index
built this way settles at about four~--- four subtrees entered at each level
instead of one~--- and stays there as the tree grows deeper, whereas
partitioning by the operator class settles at about $1.3$. The index is built
several times faster, occupies less space, and answers point queries twice as
expensively, consistently across an order of magnitude in data volume. A
comparison of construction methods by construction time~--- the comparison such
papers customarily report~--- hides this entirely.

\paragraph{Contributions.}

\begin{enumerate}
\item We formulate bulk loading as a framework-level problem and show that a
      single comparator-returning support function is sufficient, so that the
      technique becomes available to an arbitrary operator class rather than to
      one key type (Section~\ref{sec:build}). Changing the space-filling order
      requires writing one function and touches neither the tree, nor logging,
      nor concurrency: we demonstrate this by supplying Hilbert order as a
      drop-in alternative to Z-order, an operator class differing from the
      built-in one in exactly one support function out of ten
      (Section~\ref{sec:eval}).
\item We show that the order and the choice of split points are two separate
      levers, both of which live in the operator class, and we measure their
      interaction. On synthetic data they are partially interchangeable: a
      better curve recovers 44\,\% of the penalty of occupancy-based packing, a
      better split rule recovers 53\,\%, and together they bring query cost
      within 4\,\% of insertion-based construction. On 138M real points the
      relation is asymmetric: the curve recovers 33\,\% when split points are
      chosen by occupancy and nothing at all when they are chosen by the
      operator class. Investing in an elaborate space-filling order therefore
      pays off only while the partitioning rule stays naive
      (Section~\ref{sec:eval}).
\item We characterise the overlap introduced by occupancy-based packing. It does
      not compound with depth, as one might expect from the fact that a node is
      visited only if its parent was; instead each strategy converges to a
      steady-state value $\omega_\infty$ within one or two levels of the root.
      We measure $\omega_\infty$ for both strategies, across data volume and
      degree of clustering (Section~\ref{sec:overlap}).
\item We give an algorithm that delegates split point selection back to the
      operator class over a buffer of $k$ pages, removing 87--89\,\% of the
      penalty, and analyse the dependence on $k$ (Section~\ref{sec:overlap}).
      The residual is the price of choosing split points within a buffer rather
      than globally, and we quantify it against the insertion-built baseline.
\item We report five years of deployment: the design ships in PostgreSQL since
      release~14 and is the default for a family of operator classes since
      release~18. We include the one case in which the correctness condition of
      the method was violated and what it implies for framework design
      (Section~\ref{sec:deployment}).
\end{enumerate}

\section{Background}
\label{sec:background}

% Обобщённое дерево поиска: узел = страница; ключ покрывает ключи поддерева;
% поиск многопутевой. Класс операторов: consistent, union, penalty, picksplit,
% same, необязательные compress/decompress/fetch, distance.
% Два свойства, существенные для статьи: (1) порядка в интерфейсе нет;
% (2) picksplit кодирует представление автора класса операторов о хорошем
% разбиении — им мы и воспользуемся вместо изобретения нового критерия.
% Конкурентность по Корнакеру: правые ссылки.

\section{Related work}

% Три группы, по каждой — почему неприменимо или чем отличается.
% (а) Упаковка R-деревьев: Roussopoulos, Kamel–Faloutsos, STR. Сформулированы
%     через координаты и площадь MBR; к классу операторов над сигнатурами или
%     диапазонами неприменимы. Наш Hilbert-компаратор показывает, что они
%     становятся частным случаем фреймворковой постановки.
% (б) Качество разбиения: R*-дерево, RR*-дерево. Оптимизируют дерево,
%     построенное вставкой; мы их критерий не заменяем, а переиспользуем.
%     Наша более ранняя попытка изменить функцию штрафа [penalty] неприменима
%     в промышленной системе: меняет форму уже построенных индексов.
% (в) Удешевление самой вставки [memory ops] — ортогонально.
% (г) Внешняя сортировка: модель Aggarwal–Vitter.

\section{Cost model}
\label{sec:cost}

% Оставить, но каждую формулу связать с измеряемой величиной, иначе украшение.
% Обозначения; P_ins; P_sort; P_point через omega_l; вывод: сравнивать методы
% построения нужно по P_build + n * P_point, а не по P_build.

\section{Sorted construction at framework level}
\label{sec:build}

\subsection{The support function}
% Один компаратор. Почему объявлять его должен класс операторов, а не параметр
% индекса: в первой редакции компаратор передавался параметром отношения, что
% позволяло построить заведомо некорректный индекс [thread-fast-build].

\subsection{Locality-preserving orders}
% Z-порядок для точек: формула. Представление IEEE 754 как целого. NaN.
% Ключевой тезис: Hilbert подставляется заменой одной функции.

\subsection{Bottom-up packing and right links}
% union один раз на страницу. Правые ссылки устанавливаются, хотя
% конкурентности нет — ради сохранения проверяемых инвариантов.

\section{Non-leaf key overlap}
\label{sec:overlap}

\subsection{Why occupancy-based packing fails}
% Рисунок 1 — главный рисунок статьи: плотная область, рассечённая границей
% страницы, против разбиения по picksplit.

\subsection{Characterisation}
% Определение качества порядка; утверждение о зависимости перекрытия от
% рассогласования границ плотных областей с ёмкостью страницы;
% предсказание, проверяемое экспериментом \ref{sec:eval-cluster}.

\begin{definition}
% Качество порядка: ожидаемый объём union для последовательности из f подряд
% идущих элементов, нормированный на объём оптимального разбиения того же
% размера.
\end{definition}

\begin{claim}
% При разбиении по заполнению страницы перекрытие уровня ограничено снизу
% величиной, зависящей от рассогласования границ страниц с границами плотных
% областей; максимум достигается, когда размер плотной области не кратен
% ёмкости страницы.
\end{claim}

\subsection{Delegating split selection to the operator class}
% Алгоритм: буфер k страниц, рекурсивное применение picksplit, запись частей.
% Выбор k — компромисс; зависимость качества и стоимости от k (эксперимент).
% Ограничение: алгоритмы разбиения суперлинейной сложности деградируют на
% буфере; для таких классов операторов метод не включается.

\section{Implementation}
\label{sec:impl}

% Коммиты 16fa9b2b30a, 6f0bc5e1daf, 265ea567852 (выпуск 14); f1ea98a7975
% (выпуск 15); e4309f73f69 (выпуск 18, способ по умолчанию).
% Обсуждения архивированы [thread-fast-build, thread-btree-gist-sort].

\section{Evaluation}
\label{sec:eval}

\subsection{Setup}
% Стенд; почему первичная метрика — число обращений к страницам, а не время
% (обоснование, а не оправдание): переносимость и прямое сопоставление с
% моделью. См. plan/50-benchmarks.md.

\subsection{Datasets and operator classes}
% Реальные: OSM точки и полигоны, поездки такси, астрономический каталог.
% Синтетические: управляемая кластеризация (\ref{sec:eval-cluster}), D = 2..32.
% Классы операторов: point, box, polygon, range, btree_gist-скаляры.

\subsection{Baselines}
% Вставка; наивная сортированная сборка (выпуск 14); сборка с picksplit
% (выпуск 15+); Hilbert-порядок как замена Z-порядка.

% Факторный план 2x2, обращений к странице индекса на точечный запрос.
%
% 1 млн равномерных (bench/results/2026-08-02-hilbert.md):
%                     | Z-порядок | Гильберт
%   по заполнению     |   9,96    |   5,57
%   picksplit         |   4,66    |   4,11
%   построение вставкой:  3,95
%
% 138 млн точек OSM Netherlands, окно калибровано на 2,7 строки в ответе,
% 5000 зондов (bench/results/2026-08-02-osm-clean.md):
%                     | Z-порядок | Гильберт
%   по заполнению     |  15,20    |  10,17
%   picksplit         |   7,91    |   8,27
%
% Отношение naive/picksplit при Z-порядке: 2,17 на синтетике, 1,92 на OSM —
% согласуется. Но взаимодействие факторов на реальных данных асимметрично:
% кривая помогает только при наивном разбиении.
%
% ВАЖНО: время построения между кривыми сравнивать нельзя, пока для Гильберта
% не реализованы сокращённые ключи (или пока они не отключены для Z-порядка).
% Разница во времени — это наличие оптимизации сортировки, а не свойство кривой.

\subsection{Construction time and index size}
% Историческая точка отсчёта: 3 млн точек, 32,1 с против 6,1 с (выпуск 13).
% Переснять.

\subsection{Overlap and query performance}
% omega_l по уровням для четырёх базовых методов; точечные, диапазонные, kNN.
% Главная таблица: наивная сборка быстрее строится и медленнее отвечает.

\subsection{Controlled clustering}
\label{sec:eval-cluster}
% Размер кластера c*f, c = 0.25 .. 8. Предсказание из \ref{sec:overlap}.
% Самый ценный эксперимент статьи.

\subsection{Sensitivity to buffer size}
% k = 1, 2, 4, 8, 16.

\subsection{Dimensionality}
% D = 2, 4, 8, 16, 32.

\subsection{Which operator classes admit a locality-preserving order}
% Отрицательный результат — тоже результат: очерчивает границу применимости.

\section{Deployment experience}
\label{sec:deployment}

% Пять лет в выпусках; с выпуска 18 — способ по умолчанию для btree_gist.
% Что сломалось: первая попытка распространить метод на btree_gist отозвана в
% день принятия; помимо ошибки в тестовом окружении, компаратор для битовой
% строки использовал сравнение, не согласованное с семантикой типа. Возврат
% состоялся четырьмя годами позже реализацией, написанной заново
% [thread-btree-gist-sort].
%
% Вывод для проектирования фреймворка (это и есть содержание раздела, а не
% рассказ о происшествии): согласованность компаратора с union и consistent —
% свойство класса операторов, которое нельзя установить тестированием сборки,
% поскольку несогласованный компаратор даёт правдоподобный индекс, теряющий
% часть ответов. Оно проверяемо только как структурный инвариант построенного
% дерева. Фреймворк, вводящий новую функцию поддержки, обязан вводить вместе с
% ней и проверку соответствующего инварианта.
% Держать в пределах половины страницы.

\section{Limitations}

% 1. Нужен порядок, сохраняющий локальность; для части классов операторов его
%    нет, и диагностики короче измерения качества запросов не существует.
% 2. Нужен алгоритм разбиения, не деградирующий на буфере в k страниц.
% 3. Корректность держится на согласованности компаратора — ответственность
%    автора класса операторов, фреймворком не обеспечивается.
% 4. Выигрыш не переносится на последующее сопровождение: вставленные после
%    построения элементы идут обычным путём.

\section{Conclusion}

% Массовая загрузка расширяемого индексного фреймворка сводится к сортировке
% при одной дополнительной функции поддержки; но сведение само по себе
% ухудшает дерево, и восстановление качества требует вернуть выбор точек
% разбиения классу операторов. Перспективы: распараллеливание сортировки;
% проверка согласованности класса операторов, а не отдельного индекса.

\section*{Acknowledgements}

The work on non-leaf key overlap (Section~\ref{sec:overlap}) started as a
distributed hackathon convened by Darafei Praliaskouski, who brought the
problem~--- a spatial index that was built quickly and queried slowly~--- and
gathered the participants. Aliaksandr Kalenik and Sergei Shoulbakov took part
in it; many alternatives were tried before the one described here was chosen,
and the discarded ones informed the design as much as the accepted one. We
thank Heikki Linnakangas, Thomas Munro, Pavel Borisov, Andres Freund, Alexander
Korotkov and Bernd Helmle for review of the implementation, and Bernd Helmle
for the \code{btree\_gist} work of Section~\ref{sec:deployment}.

% Соавторство: авторами остаются Бородин и Поршнев, участники хакатона названы
% здесь; для защиты вклад по этому разделу считается личным (применён патч
% автора).
% TODO: получить экспорт чата хакатона — оттуда раздел «отвергнутые
% альтернативы», которого сейчас нет и который рецензенты ценят.

\bibliographystyle{plain}
\bibliography{../../common/refs}

\end{document}
