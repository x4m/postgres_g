# Статьи

Каталог на статью, в каждом `main.tex`, общая библиография — `../../common/refs.bib`.
Сборка: `make -C thesis papers` или `tectonic -X compile tex/papers/<id>/main.tex --outdir build/<id>`.

| ID | Статья | Положение | Состояние |
|----|--------|-----------|-----------|
| p1-sorted-build | Sorting-based construction of generalized search trees | П3 | **черновик целиком**, кроме измерений (§9) |
| p2-gist-vacuum | Concurrent garbage collection in generalized search trees | П4 | каркас с содержанием разделов |
| p7-cost-models | Модели стоимости доступа к многомерным данным (рус., ВАК) | П1 | каркас, формулы расставлены |

Ещё не заведены: P3 (верификация, П5), P4 (ветвистость, П2), P5, P6, P8, P9 —
см. `../../plan/20-publications.md`.

## Правила

- Ни одно число не переносится из переписки или из прежних публикаций без
  перепроверки на актуальном выпуске; исторические значения приводятся с явной
  пометкой, к какой версии относятся.
- Каждый результат сопровождается идентификатором коммита и ссылкой на
  публичное обсуждение (`thread-*` в `refs.bib`).
- Вместе со временем построения всегда приводится время выполнения запросов и
  перекрытие ключей: по (2) стоимость поиска можно ухудшить, улучшая построение.
- Отменённые результаты не замалчиваются: разбор отката — отдельный раздел
  (p2, §7).
