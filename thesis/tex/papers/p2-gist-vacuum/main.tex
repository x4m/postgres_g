% P2 — Concurrent garbage collection in generalized search trees.
% Закрывает положение П4. Материал: выпуски 12, 13, 18.
% Целевое издание: Journal of Systems and Software (Elsevier), Q1.
% Почему не Information Systems, куда подана P1: во-первых, разбор отката —
% жанр программной инженерии, в индексной статье он читается как отступление;
% во-вторых, две одновременные подачи в один журнал связывают риск.
% Аннотация выстроена под JSS: сначала эксплуатация и отменённый результат,
% затем алгоритмы как то, что придаёт им вес.
%
% В статью обязательно включается разбор случая JPCS 2018 (раздел
% «Lock granularity, and a result that did not survive»). Это то, ради чего
% статью стоит подавать в журнал по программной инженерии, а не только по
% базам данных: отрицательный результат с методологическим выводом.
%
% Один автор, как и в P1. Люди, с которыми писался код, названы в
% благодарностях, а не в списке авторов.

\documentclass[11pt, a4paper]{article}

\usepackage{fontspec}
\usepackage[english, russian]{babel}
\selectlanguage{english}
\setmainfont{Times New Roman}[Ligatures=TeX]
\setmonofont{Menlo}[Scale=0.85]

\usepackage{geometry}
\geometry{a4paper, margin=25mm}
\usepackage{amsmath, amssymb, graphicx, booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[hidelinks]{hyperref}
\newcommand{\code}[1]{\texttt{#1}}
\emergencystretch=1em

\title{Concurrent garbage collection in generalized search trees}
\author{Andrey Borodin}
\date{}

\begin{document}
\maketitle

\begin{abstract}
This paper reports eight years of building, shipping and operating concurrent
garbage collection for an extensible index framework, and one case in which that
went wrong in an instructive way. A lock-granularity algorithm of ours was
published, peer-reviewed, committed, deployed to production, and then reverted
across every supported release after a report from the field: the algorithm was
correct in its own terms, but it assumed something about the rest of the system
that did not hold, and no reviewer of the paper was in a position to know that.
We use the case to argue that peer review of a concurrent algorithm establishes
less than it appears to, and that the gap is closed not by more tests but by
invariants checked over the structure itself.

The substrate for that argument is a body of work in PostgreSQL's generalized
search trees, released and in production since version~12. Maintenance that
traverses index pages in physical rather than logical order, with the treatment
of entries that concurrent insertions move behind the sweep: it halves index page
reads exactly, and cuts index cleanup time by 3.43 on a network-backed volume~---
an advantage we show experimentally to be created entirely by operating system
readahead, and which an independent report on a rotating disk put at fifty. A
two-pass algorithm that unlinks and reclaims emptied pages, turning unbounded
index growth under a sliding-window workload into growth 51 times slower; its
correctness rests on the order of two operations under coupled locks. And a
safety condition for page reuse whose failure mode, when expressed over a
wrapping counter, is not corruption but reclamation silently ceasing after long
uptime~--- a defect that no test of bounded duration can find, and that took
months of operation to surface.

Every defect discussed here was found by operation and none by a test. We report
what that exposure found, what it did not, and what we now do differently.
\end{abstract}

\input{body}

\section*{Acknowledgements}

% TODO: назвать по именам людей из тредов — рецензентов и соавторов кода.
% Источник: plan/10-contributions.md.

\bibliographystyle{plain}
\bibliography{../../common/refs}

\end{document}
