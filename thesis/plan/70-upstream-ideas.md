# Побочные находки: кандидаты в коммиты

Обнаружено в ходе измерений для P1. Каждый пункт — самостоятельное улучшение,
не зависящее от диссертации.

## 1. `gist_page_items` неверно декодирует ключи (дефект)

**Приоритет: высокий, это дефект, а не улучшение.**

`contrib/pageinspect/gistfuncs.c`, функция `gist_page_items` разбирает
индексные кортежи дескриптором самого индекса:

```c
if (flagbits & F_LEAF)
    tupdesc = RelationGetDescr(indexRel);
else
    tupdesc = CreateTupleDescTruncatedCopy(RelationGetDescr(indexRel),
                                           IndexRelationGetNumberOfKeyAttributes(indexRel));
```

Усечение отбрасывает покрывающие атрибуты, но тип ключевых атрибутов остаётся
типом столбца. Между тем в GiST класс операторов может объявлять `STORAGE`
другого типа, и тогда на странице лежит не то, что говорит дескриптор.

Для `point_ops` (`STORAGE box`) на внутренней странице лежит `BOX` — четыре
`float8`, 32 байта, — а декодируется он как `point`, то есть 16 байт. Вывод
получается синтаксически правильный и молча неверный: печатается только угол
`high`, угол `low` теряется без всякого признака ошибки:

```
=# select keys from gist_page_items(get_raw_page('ti',0),'ti') limit 1;
              keys
---------------------------------
 (p)=((0.99987476,0.011655656))     -- на самом деле это BOX, показан один угол
```

Затронуты все классы операторов, у которых `opckeytype` отличается от типа
столбца: `point_ops`, `tsvector_ops` (`gtsvector`), большинство классов в
`contrib` (`ltree`, `hstore`, `intarray`, `pg_trgm`, `cube` с сигнатурами).

Исправление: если `pg_opclass.opckeytype` не нулевой, строить дескриптор по
типу хранения. Для листовых страниц — тоже: там ключ той же природы, просто у
`point_ops` бокс вырожден и совпадение случайно.

Практическое следствие: средством нельзя пользоваться для отладки и проверки
внутренних страниц, а именно за этим к нему и обращаются. При работе над P1
пришлось восстанавливать ключи внутренних узлов косвенно — снизу вверх, из
листовых.

Смежный прецедент: `fa41cf8f183` «Avoid misinterpreting GiST pages in
pageinspect» — тот же класс проблем.

*Перед отправкой: воспроизвести на master (проверено чтением кода, не
запуском), проверить `tsvector_ops` и хотя бы один класс из `contrib`.*

## 2. Кривая Гильберта как порядок для сортированного построения

**Приоритет: средний, измеримый выигрыш.**

Штатный `gist_point_sortsupport` использует Z-порядок Мортона. Замена на
кривую Гильберта снижает число обращений к страницам индекса на точечный
запрос на 12 % при разбиении через `picksplit` и на 44 % при разбиении по
заполнению страницы (измерения `bench/results/2026-08-02-hilbert.md`).

Причина понятна: у кривой Гильберта соседние по кривой точки всегда соседние в
пространстве, у Z-порядка — не всегда, разрывы дают «хвосты» в ключах страниц.

Что нужно до отправки: реализовать сокращённые ключи (abbreviated keys), как у
Z-порядка, иначе построение существенно медленнее и предложение отклонят по
времени сборки. Совместимость: меняется форма вновь строящихся индексов, старые
продолжают работать, перестроение не требуется — то есть ограничение, из-за
которого не прошло изменение функции штрафа, здесь не действует.

Открытый вопрос для обсуждения в списке рассылки: менять существующий
`point_ops` или добавить отдельный класс операторов. Первое лучше для
пользователей, второе безопаснее.

## 3. `GIST_SORTED_BUILD_PAGE_NUM` = 4 не оптимально

**Приоритет: средний. Измерено, патч тривиален, но улучшение не бесплатное.**

`f1ea98a7975` накапливает четыре страницы и применяет к ним `picksplit`.
Константа зашита. Измерена чувствительность (`bench/results/2026-08-02-bufsize.md`,
10 млн равномерных точек, варианты собраны из одного дерева):

| $k$ | сборка, с | страниц | обращений на запрос |
|---|---|---|---|
| 1 | 10,25 | 54 351 | 12,65 |
| 2 | 14,44 | 81 593 | 10,62 |
| **4** | **20,36** | **81 287** | **6,37** |
| 8 | 19,92 | 81 682 | 5,59 |
| 16 | 23,58 | 81 650 | 5,30 |

**Поправка 2026-08-03:** утверждение «не стоит времени построения» было
неверным, оно опиралось на однократный прогон, где $k = 4$ дал выброс 20,36 с.
С тремя прогонами и чередованием (`bench/results/2026-08-03-buildtime-repeats.md`)
медианы составляют 8,9 / 13,1 / 15,7 / 20,0 / 23,4 с для $k = 1, 2, 4, 8, 16$.

**Переход с 4 на 8 даёт 12 % по числу обращений ценой 27 % времени
построения.** Это размен, а не бесплатное улучшение, и как предложение в
основную ветку оно существенно слабее: выигрывает тот, кто строит индекс
однократно и много читает; кто строит часто — проигрывает.

Объём индекса от $k$ не зависит начиная с двойки.

Заодно видно, что $k = 1$ воспроизводит разбиение по заполнению страницы
(12,65 против 12,23 у кода до `f1ea98a7975`), то есть $k$ — непрерывный
параметр между двумя стратегиями.

Перед отправкой: проверить на классах операторов с более дорогим `picksplit`,
чем `gist_box_picksplit` — именно из-за них константа выбиралась осторожно;
убедиться, что `ready_pages[XLR_MAX_BLOCK_ID]` не переполняется.

## 4. Сортированная сборка GiST не соблюдает fillfactor

**Приоритет: высокий. Подтверждено кодом, измерением и перепиской. В выпусках
с 15-го, то есть четыре года.**

До `f1ea98a7975` накопление кортежей на страницу учитывало запас, заданный
`fillfactor`:

```c
sizeNeeded = IndexTupleSize(itup) + sizeof(ItemIdData) + state->freespace;
```

После — нет, и это прямо отмечено в комментарии:

```c
sizeNeeded = IndexTupleSize(itup) + sizeof(ItemIdData); /* fillfactor ignored */
```

`buildstate.freespace` по-прежнему вычисляется из `fillfactor`, но в
сортированном пути больше нигде не используется. Измерение подтверждает: индекс
на 10 млн точек занимает 81 326 страниц и при значении по умолчанию, и при
`fillfactor = 65` (`bench/results/2026-08-03-fillfactor-control.md`).

### Это было осознанное решение, а не недосмотр

Переписка рабочей группы, январь 2022:

- 22.12.2021, А. Каленик: «сейчас филлфактор учитывается только при добавлении
  элемента в страницы, которые будут переданы в сплит, который не знает про
  филлфактор, и получается, что он по сути просто не учитывается»;
- 13.01.2022, он же: «а нужно ли учитывать fillfactor при заполнении страниц,
  которые накапливаются для сплита? я думаю, что это имеет смысл только когда
  накапливание отключено»;
- Д. Пролесковский: «лучшее что мы можем сделать — сплит с priority queue, пока
  средний филлфактор всей группы не нырнёт ниже заданного. а пока копим, копить
  до конца страниц»;
- С. Шулбаков: «gistSplit не учитывает fillfactor… можно не учитывать fillfactor
  в gist\_indexsortbuild, если кол-во страниц > 1, но передавать GiSTOptions в
  gistfitpage и учитывать там»; и следом: «достаточно ли пока перестать
  учитывать fillfactor для >1?» — «я думаю что да».

То есть решение принято как временное («пока»), с уже намеченным правильным
устройством, и с тех пор к нему не возвращались. Корень глубже самой
сортированной сборки: `gistSplit` и `gistfitpage` о `fillfactor` не знают
вообще, поэтому обычный путь расщепления его тоже соблюдает лишь частично.

### Почему это всё-таки стоит починить

Документация обещает обратное: «The other index methods use fillfactor in
different but roughly analogous ways». Пользователь, задавший `fillfactor = 50`,
чтобы оставить место под будущие обновления, получает полностью упакованный
индекс и платит расщеплениями на первых же обновлениях. Ни диагностики, ни
предупреждения, ни оговорки в документации нет.

### Два пути

1. Передавать `GiSTOptions` в `gistfitpage`, чтобы условие «часть помещается на
   страницу» стало «часть помещается в `BLCKSZ - freespace`». Рекурсия в
   `picksplit` для этого уже есть, меняется условие останова. Чинит и обычный
   путь расщепления заодно.
2. Вариант с очередью с приоритетом: расщеплять, пока средний коэффициент
   заполнения группы не опустится ниже заданного. Точнее, но заметно больше
   работы.

Минимальный честный шаг, если ни один из путей не выбран, — оговорить в
документации, что сортированная сборка `fillfactor` не соблюдает. Сейчас
поведение расходится с описанием молча.

## 5. Уровень страницы не виден снаружи

**Приоритет: низкий.**

В GiST уровень на странице не хранится, и `gist_page_opaque_info` его не
показывает. Любое средство анализа — `amcheck`, `pageinspect`, скрипты — вынуждено
размечать уровни обходом в ширину от корня. Менять формат страницы ради этого
не стоит, но `pageinspect` мог бы отдавать вычисленный уровень отдельной
функцией, раз обход всё равно кому-то нужен.

## 6. `probe`-подобной статистики по индексу нет

**Приоритет: обсуждаемый.**

Чтобы измерить качество дерева (перекрытие ключей), пришлось писать
восстановление ключей на plpgsql. Штатной возможности спросить у индекса
«сколько узлов уровня накрывают заданную точку» нет, хотя это ровно та
величина, которая определяет стоимость поиска. Кандидат в `pageinspect` или в
отдельное расширение: функция, возвращающая распределение перекрытия по
уровням. Полезна и разработчикам классов операторов — сейчас качество своего
`picksplit` проверить нечем.
