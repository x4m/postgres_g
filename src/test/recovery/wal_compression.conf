# Exercise WAL compression throughout the recovery tests.
#
# pglz is used deliberately: it is always available, so this needs no guard for
# builds configured without --with-lz4 or --with-zstd.  052_wal_compression.pl
# covers lz4 and zstd separately, skipping whichever a build lacks.
wal_compression = 'pglz'
